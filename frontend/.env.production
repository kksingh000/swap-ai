# Baked in at build time by Vite. Points the deployed frontend at the Render
# backend. Keep this in sync with the render.yaml service name below.
VITE_API_BASE=https://swap-ai-backend.onrender.com/api
