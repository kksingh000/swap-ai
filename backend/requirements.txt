# ---- Core (required) --------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy>=2.0.30
httpx>=0.27
python-multipart>=0.0.9
apscheduler>=3.10,<4
tzdata>=2024.1

# ---- Lead classifier (Phase 5; the app runs fine without these) -------
scikit-learn>=1.5
joblib>=1.4
numpy>=1.26

# ---- Optional extras --------------------------------------------------
# psycopg[binary]>=3.2        # PostgreSQL in production
# faster-whisper>=1.0         # server-side STT (FREE, local)
# redis>=5.0                  # if you move the action queue off-process

# ---- Dev / test -------------------------------------------------------
pytest>=8.2
pytest-asyncio>=0.23
